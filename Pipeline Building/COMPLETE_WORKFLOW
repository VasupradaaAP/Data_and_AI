                         WEATHER DATA PIPELINE – COMPLETE FLOW

 ┌──────────────────────┐
 │      config.py       │
 │  - API settings      │
 │  - City coordinates  │
 │  - BigQuery config   │
 │  - Logging config    │
 └──────────┬───────────┘
            │
            ▼
 ┌──────────────────────┐
 │      main.py         │
 │  Pipeline Orchestror │
 └──────────┬───────────┘
            │
            ▼
 ┌────────────────────────────────────┐
 │ STEP 1 : Fetch Weather Data        │
 │        api_client.py               │
 │------------------------------------│
 │ • Call Open-Meteo API              │
 │ • Build request parameters         │
 │ • Retry on failure                 │
 │ • Validate API response            │
 └──────────┬─────────────────────────┘
            │
            ▼
 ┌──────────────────────┐
 │   Raw JSON Response  │
 └──────────┬───────────┘
            │
            ▼
 ┌────────────────────────────────────┐
 │ STEP 2 : Transform & Clean Data    │
 │      data_transformer.py           │
 │------------------------------------│
 │ • Flatten nested JSON              │
 │ • Type conversion                  │
 │ • Handle missing values            │
 │ • Remove duplicates                │
 │ • Validate temperature ranges      │
 │ • Add derived analytics            │
 │    - comfort_index                 │
 │    - precipitation_risk            │
 │    - weather_description           │
 │    - day_type                      │
 │    - temporal features             │
 └──────────┬─────────────────────────┘
            │
            ▼
 ┌──────────────────────┐
 │   Clean DataFrame    │
 └──────────┬───────────┘
            │
     ┌──────┴───────────────┐
     ▼                      ▼
┌───────────────┐   ┌─────────────────────┐
│ Save CSV/JSON │   │ Upload to BigQuery  │
│   output/     │   │ bigquery_client.py  │
└──────┬────────┘   └──────────┬──────────┘
       │                       │
       │                       ▼
       │          ┌────────────────────────┐
       │          │ • Authenticate         │
       │          │ • Create dataset       │
       │          │ • Create table         │
       │          │ • Validate schema      │
       │          │ • Load DataFrame       │
       │          └──────────┬─────────────┘
       │                     │
       ▼                     ▼
┌────────────────────────────────────┐
│ STEP 5 : Logging & Quality Checks  │
│------------------------------------│
│ • Generate logs                    │
│ • Validate missing values          │
│ • Check load success               │
│ • Store execution history          │
└──────────┬──────────────────────── ┘
            │
            ▼
 ┌──────────────────────┐
 │  Final Outputs       │
 │----------------------│
 │ • CSV files          │
 │ • JSON files         │
 │ • BigQuery table     │
 │ • Pipeline logs      │
 └──────────────────────┘
